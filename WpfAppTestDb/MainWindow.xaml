<Window x:Class="WpfAppTestDb.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppTestDb"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <!--<DataGrid Name="mainGameData" />-->
        <DataGrid AutoGenerateColumns="True" x:Name="mainGameData" Margin="5 50 5 5"  >
            <!--<DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="ID" Width="50"  />
                <DataGridTextColumn Binding="{Binding GameDescription.Game.Name}" Header="Title" Width="100"  />
                <DataGridTextColumn Binding="{Binding Description}" Header="Desc" Width="100"  />
               

            </DataGrid.Columns>-->
            
        </DataGrid>

    </Grid>
</Window>
